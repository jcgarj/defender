{% extends '::base.html.twig' %}

{% block body -%}
    <h1>memoria_ram list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marca</th>
                <th>Capacidad</th>
                <th>Tipo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('memoria_ram_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.marca }}</td>
                <td>{{ entity.capacidad }}</td>
                <td>{{ entity.tipo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('memoria_ram_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('memoria_ram_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('memoria_ram_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
