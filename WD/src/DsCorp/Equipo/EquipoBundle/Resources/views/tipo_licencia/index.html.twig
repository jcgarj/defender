{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tipo_licencia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Precio</th>
                <th>Meses</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipo_licencia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.precio }}</td>
                <td>{{ entity.meses }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipo_licencia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipo_licencia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipo_licencia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
